# 企业级 RAG 开发清单

> 目标：构建可跨文档回答、严格受权限约束、可定位原文、可评测并能控制 Token 的本地企业知识库。

## 总体原则

- 只根据被授权且当前有效的文档版本回答。
- 每条事实性结论必须能定位到原文页码或段落。
- 证据不足、证据冲突或权限不足时拒答或明确标记不确定性。
- 先建立评测和可观测能力，再持续调优模型、阈值和切分策略。

## Phase 0：基线与数据模型（必须先完成）

### 0.1 文档与版本数据模型

- [ ] 在 MySQL/PostgreSQL 建立 `documents` 表：`tenant_id`、`workspace_id`、`document_id`、文件名、类型、当前版本、状态、创建/更新时间。
- [ ] 建立 `document_versions` 表：版本号、文件哈希、解析状态、索引状态、知识库修订号 `knowledge_base_revision`。
- [ ] 新版本完成索引后，原版本立即设为 `is_current=false`；检索只允许 `is_current=true`。
- [ ] 删除文档或撤销授权后，立即使其逻辑不可检索；异步清理向量、对象存储文件和缓存。

验收：同一文档上传新版后，旧版不能出现在任何新回答的引用中。

### 0.2 Chunk 元数据模型

- [ ] 扩展 Qdrant payload：`tenant_id`、`workspace_id`、`document_id`、`version`、`is_current`。
- [ ] 保存定位信息：`page`、`heading_path`、`paragraph_id`、`bbox`（若解析器可提供）、`source_uri`。
- [ ] 保存质量信息：`content_kind`、`parse_confidence`、`ocr_confidence`、`indexed_at`。
- [ ] 保存权限信息：`allowed_principal_ids`，例如 `user:42`、`group:legal`。
- [ ] 为 `tenant_id`、`workspace_id`、`document_id`、`is_current` 和常用权限过滤字段创建 payload index。

验收：任意引用均可定位到“文档 + 版本 + 页码/段落”。

### 0.3 权限边界

- [ ] 从 JWT/服务端会话获取 `tenant_id`、用户 ID、用户组，不信任前端传入的权限字段。
- [ ] 在每次 Qdrant 查询中强制添加租户、当前版本和段落权限过滤。
- [ ] 所有缓存键加入 `tenant_id`、权限摘要和 `knowledge_base_revision`。
- [ ] 编写跨租户、跨用户组和已撤销权限的自动化测试。

验收：无权限用户无法通过问题、文档 ID、缓存或引用链接读取任何受限内容。

## Phase 1：文档解析与企业级切分

### 1.1 统一解析中间格式

- [ ] 将 MinerU 输出保存为可重放的结构化 Markdown/JSON，而非只保存纯文本。
- [ ] 保留标题层级、段落、列表、表格、页码、图片说明、脚注与 OCR 置信度。
- [ ] 解析、切分、嵌入拆分为独立任务，方便替换切分策略并重建索引。

验收：无需重新解析原文件，即可用不同切分策略重建同一文档的索引。

### 1.2 按文档类型切分

- [ ] **PDF/Docx 默认策略**：按标题和自然段切分；把标题路径加入每个子块的嵌入文本。
- [ ] **合同策略**：按章节、条、款切分；保留条款编号、定义条款和相邻条款引用。
- [ ] **Xlsx 策略**：按 sheet、表格和行组切分；重复表头并保存 sheet/行列坐标。
- [ ] **扫描件兜底策略**：按句子边界切分；仅在超出最大长度时截断并保留少量句子重叠。
- [ ] 建立父子 chunk：子块用于召回，父块用于补充上下文。

建议初始实验参数：子块 150–350 tokens，父块 800–1,500 tokens；不要把这些数值固定为长期规则。

### 1.3 OCR 质量策略

- [ ] `>= 0.90`：正常检索和回答。
- [ ] `0.75–0.90`：允许召回，回答与引用处展示 OCR 风险提示。
- [ ] `< 0.75`：检索降权；若无更可靠证据仍被使用，答案必须标记“内容可能存在 OCR 误差”。
- [ ] 替换当前直接过滤低于 0.7 置信度 chunk 的逻辑，改为可追溯的质量策略。

验收：低 OCR 置信度来源不会被伪装成高可信事实。

## Phase 2：检索、重排与 Token 优化

### 2.1 查询路由

- [ ] 实现零/低成本规则路由：文档列表、上传状态、精确页码、文件名和编号查询直接走数据库或关键词检索。
- [ ] 将问答分为：精确查找、事实问答、总结归纳、对比、多文档问题和无法回答。
- [ ] 仅将需要生成的任务发送给大模型。

验收：文档状态和精确定位问题不调用聊天模型。

### 2.2 混合检索与重排

- [ ] 保留现有稠密向量检索。
- [ ] 新增关键词/BM25 或 Qdrant sparse vector 检索，覆盖编号、人名、日期、型号和条款号。
- [ ] 用 RRF 融合两路候选。
- [ ] 接入已配置但未使用的 `qllama/bge-reranker-v2-m3`，只重排小候选集。
- [ ] 先以“向量 Top 30 + 关键词 Top 30 → 融合 Top 20 → rerank → 最终 Top 4–6”作为基线；通过黄金集调参。

验收：合同编号、日期、专有名词类问题的 Recall@10 高于纯向量基线。

### 2.3 上下文与缓存预算

- [ ] 设置每次问答 3,000–4,000 tokens 的证据预算。
- [ ] 对最终候选做重复消除、相邻块合并和父块按需展开。
- [ ] 多轮对话仅保留当前主题最近 4–6 轮，并生成服务端摘要记忆。
- [ ] 建立检索缓存；缓存键包含规范化问题、租户、权限摘要、文档修订号、检索配置版本。
- [ ] 谨慎建立答案缓存；答案缓存键必须额外包含模型和 Prompt 版本。

验收：在同等正确率下，平均上下文 Token 比当前 Top-12 直传方案降低至少 30%。

## Phase 3：防幻觉、引用与回答策略

### 3.1 证据门控

- [ ] 将“是否有 citation”升级为可配置证据门槛：重排分、来源质量、定位完整性、来源数量和冲突状态。
- [ ] 无充分证据、无权限、文档已失效时直接拒答。
- [ ] 出现来源冲突时返回 `status=conflict`，展示冲突来源，不强行合成结论。

### 3.2 结构化回答与引用校验

- [ ] 让模型输出 `answer`、`claims`、`citation_ids`、`confidence`、`status`。
- [ ] 服务端校验每个 `citation_id` 是否属于本次检索最终证据集。
- [ ] 每条事实性 claim 必须至少有一个引用；否则剔除、降级为推断或拒答。
- [ ] 明确区分“知识库事实”和“模型建议/推断”。
- [ ] 前端实现引用点击：打开文档、精确页码、段落/坐标高亮。
- [ ] 增加“该引用不支持答案”的反馈入口。

验收：人工抽样中，95% 以上的事实性句子具有真实支持它的引用。

## Phase 4：评测、可观测性与可靠性

### 4.1 黄金评测集

- [ ] 首版建立 80 条人工标注问题。
- [ ] 覆盖：40 条可回答、15 条应拒答、10 条跨文档、5 条版本冲突、5 条 OCR 风险、5 条权限隔离。
- [ ] 每条记录标注：问题、授权范围、标准证据 chunk、答案要点、是否应拒答。
- [ ] 建立评测脚本，支持不同切分、embedding、检索和 reranker 配置的横向对比。

### 4.2 指标与发布门禁

- [ ] 检索：Recall@K、MRR/NDCG。
- [ ] 回答：事实忠实度、引用支持率、拒答精确率与拒答召回率。
- [ ] 性能：检索耗时、重排耗时、首 Token、完整回答 P95、平均 Token。
- [ ] 每次发布记录：解析器版本、切分策略版本、embedding 模型、reranker、阈值、Prompt、知识库修订号。
- [ ] 任一关键指标相对基线下降时阻止发布。

建议起始门槛：Recall@10 ≥ 90%，引用支持率 ≥ 95%，拒答精确率 ≥ 95%。实际阈值应在积累黄金集后校准。

### 4.3 最小可观测性

- [ ] 记录 request ID、用户/租户、问题类别、过滤条件、候选数量、重排分和最终 chunk ID。
- [ ] 记录上下文 Token、模型耗时、总耗时、拒答原因、用户反馈。
- [ ] 为 Qdrant、Ollama 和 MinerU 设置健康检查。
- [ ] 定义降级：Qdrant 不可用则拒答；Ollama 不可用则返回服务不可用；MinerU 不可用则上传任务排队重试。

## 推荐排期

1. **第 1–2 周**：Phase 0、解析中间格式、标题/段落/表格切分。
2. **第 3–4 周**：段落级权限、版本切换、引用定位、OCR 风险提示。
3. **第 5–6 周**：混合检索、reranker、上下文预算、查询路由。
4. **第 7 周**：防幻觉结构化回答、冲突处理、引用反馈。
5. **第 8 周及以后**：黄金集、回归评测、缓存、性能优化和审计能力。

## 当前代码的第一批改动点

- [ ] `services/python-rag/app/vector_store.py`：扩充 chunk payload，增加权限、版本和质量过滤。
- [ ] `services/python-rag/app/graph.py`：增加查询路由、重排、上下文预算和增强版 evidence gate。
- [ ] `services/python-rag/app/models.py`：扩展 Citation 与 QueryResponse，加入 chunk ID、定位、风险与结构化 claims。
- [ ] `services/python-rag/app/settings.py`：将 Top-K、阈值、Token 预算、模型版本配置化。
- [ ] `services/python-rag/tests/`：新增权限、拒答、版本、引用、OCR 与检索回归测试。
- [ ] `apps/web`：引用原文预览、高亮定位、冲突态、OCR 风险提示和引用反馈。
