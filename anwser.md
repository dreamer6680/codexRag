1. 业务边界与权限
你的首批企业用户是谁：销售、客服、研发、法务、运营，还是管理层？他们最常问的 10 类问题分别是什么？

暂时不定，依据用户的文档来，我不设定用户的角色，上传什么文档，就解析什么问题就可以了。

回答是否必须限定在某个知识空间、部门、项目、客户或文档版本内？
不需要，可以获取用户所有相关的内容，进行联合回答，但是文档版本需要保证最新。

用户是否只能检索自己有权限的文档？是否需要支持“文档级、段落级、字段级”权限？
是的，需要支持段落级。

一份文档更新、删除、撤销授权后，旧向量、缓存答案、引用链接必须在多长时间内失效？
依据性能决定，我需要你进行评估

是否需要租户隔离？Qdrant collection、对象存储和数据库应是“每租户独立”还是“共享但强过滤”？
这个是什么问题，我不太懂，需要你解释一下

是否需要审计：谁问了什么、系统检索了哪些片段、模型用什么证据回答、是否被用户采纳？
需要审计，可以考虑延后做，这个是升级功能，初版我们不考虑

2. 文档解析与知识治理
哪些文档类型是核心：合同、制度、产品文档、扫描 PDF、表格、邮件、图片？哪一类的错误代价最高？
PDF解析，doc，xlsx都是核心，错误代价最高的应该是合同，但是合同一般也是pdf解析的，所以理论上是pdf解析最重要


MinerU 解析后，表格、标题层级、页码、图片说明、脚注、OCR 置信度是否需要保留为结构化元数据？
这个我不太理解，需要你做解释

文档切分应以固定长度为主，还是按标题、段落、表格、语义边界切分？不同文档类型是否应有不同策略？
我不希望是固定长度，固定长度不好，我希望你给我一个企业级的知识库检索切分策略，我需要的是多个方案进行评估

一个片段需要携带哪些最小元数据：文档版本、页码、标题路径、段落坐标、权限、时间、作者、可信等级？
这个我都是希望你给我一些背景方案，再做定夺

低置信 OCR 内容是直接排除、降权召回，还是允许回答但必须标记“不确定”？
允许回答，但必须标记“不确定”

是否需要人工质检队列：低质量解析、重复文档、冲突版本、敏感信息、无法定位原文的引用？
？这个暂时不做

3. 检索优化与降低 Token
哪些问题无需走大模型：精确查找、文档列表、页码定位、关键词匹配、元数据筛选？能否先用轻量路由分类？


是否需要混合检索：稠密向量 + BM25/关键词 + 元数据过滤？例如制度编号、人名、产品型号、日期通常更适合关键词检索。
查询是否需要改写或拆解？例如“新员工报销和出差有什么要求”应拆为两条检索，还是保留原始语义？
检索 Top-K、重排 Top-N、最终送入模型的片段数各是多少？是否按问题复杂度动态调整，而不是固定 Top-12？
每轮问答允许多少上下文 Token？是否要先做片段压缩、去重、相邻片段合并，再交给模型？
多轮对话中，历史消息怎样裁剪：全量保留、摘要记忆、只保留当前主题，还是仅用用户明确引用的上下文？
是否要建立“检索缓存”和“答案缓存”？缓存键是否需包括租户、权限、文档版本、模型版本和知识库版本？
是否需要在本地小模型和较大模型之间路由：简单抽取走小模型，复杂归纳走大模型，证据不足则不调用模型？
4. 减少幻觉与可验证答案
什么情况下系统必须拒答：无证据、证据冲突、相关性不足、文档过期、权限不足、OCR 不可靠？
你的“有证据”标准是什么？仅有向量相似度不够时，是否要求重排分数、关键词命中、来源数量、页码可定位同时满足？
每一条事实性结论是否都必须带引用？一个引用能否支持多个结论？如何识别“引用存在但不支持该句”的伪引用？
文档间出现冲突时，按什么规则处理：最新版本优先、权威级别优先、人工确认优先，还是必须呈现冲突而不下结论？
是否允许模型使用常识补全？如果允许，如何明确区分“来自知识库的结论”和“模型的建议/推断”？
用户点击索引后，是否必须打开精确页码与段落，并高亮原句？是否要支持“该引用不支持答案”的一键反馈？
5. 评测、性能与可靠性
你能否准备一套 100–300 条黄金问答集，覆盖：可回答、应拒答、跨文档、多跳推理、冲突、OCR 错误、权限隔离和时效性？
企业验收指标是什么？建议至少明确：Recall@K、MRR/NDCG、引用正确率、答案忠实度、拒答准确率、P95 延迟、单问 Token 和单问成本。
哪个指标优先级最高：答对率、引用正确率、拒答安全性、吞吐、响应时间，还是成本？它们冲突时如何取舍？
评测应如何分层：解析质量 → 切分质量 → 检索质量 → 重排质量 → 生成忠实度 → 端到端体验？
是否要对每次发布做回归测试，并记录 embedding 模型、切分策略、阈值、重排模型和 Prompt 的版本？
高并发目标是多少？例如 20、100 或 1000 个并发用户；P95 首字/完整回答延迟目标分别是多少？
当 Qdrant、Ollama、MinerU 任一服务不可用时，系统应该拒答、降级搜索、排队重试，还是展示已有缓存？
是否需要可观测性：每次问答的检索耗时、候选数、重排分数、上下文 Token、模型耗时、拒答原因和用户反馈？